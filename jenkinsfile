pipeline {
    agent any

    stages {

        stage('Compile') {
            steps {
              script
                  {
                //Compile your Java code
                echo " compile"
                sh"mvn compile" }}
            }
        }
    }
