pipeline {
    agent any

    stages {
        stage('package') {
            steps {
              script{
                echo 'Hello World'
                sh "mvn package" }
            }
        }
    }
}
